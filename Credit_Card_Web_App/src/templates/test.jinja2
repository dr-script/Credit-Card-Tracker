{% extends "base.jinja2" %}
{% block content %}
    <div align="center">
        <h4>Today is: {{ current_date }}</h4>
        <h3><a href="#"><span class="glyphicon glyphicon-backward"> </span></a>
            {{ date_now_future }}
            <a href="#"><span class="glyphicon glyphicon-forward"></span></a>
        </h3>
    <br>
        {% for info in data %}
            <h4>{{ info.date }} {{ info.description }} ${{ info.price }}</h4>

        {% endfor %}
    {{ x }}
    </div>

{% endblock %}