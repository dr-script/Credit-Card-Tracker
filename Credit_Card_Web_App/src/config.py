DEBUG = True # This is "True" because "debug=True" in our run.py file.
ADMINS = frozenset([ # Set that has no duplicate values
    'yourname@yourdomain.com' # Notify admins if something goes wrong
])